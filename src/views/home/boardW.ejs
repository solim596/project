<!DOCTYPE html>
<html>

<head>
    <title>글쓰기</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .container {
            width: 80%;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
        }

        form {
            text-align: center;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 15px;
        }

        textarea {
            height: 200px;
        }
        /* 푸터 스타일링 */
        footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 20px 0;
            position: fixed;
            /* 화면 하단으로 고정 */
            bottom: 0;
            /* 화면 하단에 배치 */
            width: 100%;
            /* 가로 폭을 100%로 설정하여 전체 너비 차지 */
        }


        /* 푸터 링크 스타일링 */
        footer a {
            color: #fff;
            /* 링크 글자색 설정 */
            margin: 0 10px;
            /* 링크 사이 여백 설정 */
            text-decoration: none;
            /* 링크 밑줄 제거 */
        }
    </style>
</head>

<body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>

    <div class="container" id="header-container"></div>
    <h1>문의사항</h1>
    <div class="container">
        <form action="/boardW" method="post">
            <label for="board_title">제목</label>
            <input type="text" id="board_title" name="board_title">
            <br>
            <label for="nickname">아이디</label>
            <input type="text" id="nickname" name="nickname">
            <br>
            <label for="content">내용</label>
            <textarea id="content" name="content"></textarea>
            <label for="board_date">날짜</label>
            <input type="date" id="board_date" name="board_date">
            <br>
            <input type="submit" value="글쓰기">
            <button type="button" onclick="cancel()">취소</button>
        </form>
    </div>

    <footer>
        <p>TOGETEAT</p>
        <nav>
            <a href="/이용약관">이용약관</a>
            <a href="#">개인정보방침</a>
            <a href="/소개">소개</a>
            <a href="#">음식점소개</a>
            <a href="#">공지사항</a>
        </nav>
        <p></p>
    </footer>

    <script>
   var headerContainer = document.getElementById('header-container');
    var xhr = new XMLHttpRequest();
    xhr.open('GET', '/header', true);
    xhr.onreadystatechange = function () {
        if (xhr.readyState === 4 && xhr.status === 200) {
            headerContainer.innerHTML = xhr.responseText;
        }
    };
    xhr.send();

    function cancel() {
        const confirmCancel = confirm('입력한 모든 내용이 삭제됩니다. 정말로 이동하시겠습니까?');
        if (confirmCancel) {
            window.location.href = '/boardL';
        }
    }
    </script>
    

</body>

</html>